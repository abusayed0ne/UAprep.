import{Body,Controller,Get,Param,Post,UseGuards}from'@nestjs/common';import{PermissionGuard,RequirePermission}from'../authorization.js';import{AuthContext,type RequestContext}from'../request-context.js';import{CommerceService}from'./commerce.service.js';
@Controller('commerce')export class CommerceController{constructor(private readonly service:CommerceService){}@Get('catalog')@UseGuards(PermissionGuard)@RequirePermission('attempt:create')catalog(@AuthContext()ctx:RequestContext){return this.service.catalog(ctx);}@Post('orders/:priceId')@UseGuards(PermissionGuard)@RequirePermission('attempt:create')order(@AuthContext()ctx:RequestContext,@Param('priceId')id:string){return this.service.createOrder(ctx,id);}@Get('entitlements')@UseGuards(PermissionGuard)@RequirePermission('attempt:create')entitlements(@AuthContext()ctx:RequestContext){return this.service.entitlements(ctx);}@Post('webhooks/payment')webhook(@Body()body:unknown){return this.service.webhook(body);}}
